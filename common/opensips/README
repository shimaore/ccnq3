* No local SQL database; use the HTTP DB module.
* User information, etc. is available in JSON form.

sudo aptitude install -y opensips opensips-dbhttp-module
sudo adduser `whoami` opensips # allows access to fifo