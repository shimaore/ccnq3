{
  "name": "ccnq3_inbound_enum",
  "version": "0.0.2",
  "description": "Carrier-ENUM for CCNQ3",
  "keywords": "",
  "author": { "name": "Stephane Alnet", "email": "stephane@shimaore.net" },
  "config": {
    "config_file": "/etc/ccnq3/host.json"
  },
  "scripts": {
    "start": "daemon -n ccnq3_inbound_enum -o daemon.debug -D \"`pwd`/node\" -r -- ./enum.coffee"
  , "stop":  "daemon -n ccnq3_inbound_enum -o daemon.debug --stop"
  },
  "dependencies": {
    "cdb": ">=0.0.12"
  , "ccnq3_config": ">=0.1"
  },
  "engines": { "node": ">=0.6.5", "coffee": ">=1.1.3" },

  "ok": true
}
